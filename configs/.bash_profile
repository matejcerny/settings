alias bu="brew update;brew outdated"
alias ll="ls -al"

# Tell ls to be colourful
export CLICOLOR=1

# Tell grep to highlight matches
export GREP_OPTIONS='--color=auto'

# Make other output nice and colorful, too
source /usr/local/etc/grc.bashrc
